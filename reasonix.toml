# Reasonix configuration.
# Resolution order: flag > ./reasonix.toml > ~/.reasonix/config.toml > built-in defaults.
# Secrets come from the environment via api_key_env; never put keys here.

config_version = 3   # schema marker for diagnostics; old versions may ignore it
default_model = "deepseek-flash"
# language      = "zh"   # ui/model language; empty = auto-detect from $LANG / $REASONIX_LANG

[agent]
# system_prompt = """..."""   # omit to use the built-in prompt for this version
# system_prompt_file = "prompts/system.md"   # overrides system_prompt when set
# max_steps         = 0   # executor tool-call rounds; 0 = no limit
# planner_max_steps = 12   # planner read-only tool-call rounds; 0 = no limit
temperature       = 0.0
auto_plan   = "off"   # off|on; off keeps plan mode manual
# reasoning_language = "zh"   # visible reasoning language: auto|zh|en
# auto_plan_classifier = "deepseek-flash"   # optional; only used for borderline tasks
soft_compact_ratio  = 0.5   # notice only; keeps cache-first prefix intact
compact_ratio       = 0.8   # try compacting when prompt reaches this fraction
compact_force_ratio = 0.9   # force compacting at this high-water mark
# keep                = ["errors"]   # compaction keep policy: errors, user_marked
# recent_keep         = 2   # minimum recent messages kept verbatim
cold_resume_prune   = true   # elide stale tool results when reopening a session past the provider cache window
# planner_model = "mimo"   # optional: enable two-model collaboration
# subagent_model = "deepseek-pro"   # optional default for runAs=subagent skills
# subagent_models = { review = "deepseek-pro", security_review = "deepseek-pro" }   # per-skill overrides
# subagent_effort = "high"   # optional default effort for subagents
# subagent_efforts = { review = "max", task = "high" }   # per-tool/skill effort overrides
# output_style = "explanatory"   # explanatory | learning | concise | custom; empty = default

[tools]
enabled = []   # empty = all built-in tools
bash_timeout_seconds = 120   # foreground safety cap; set 0 for no tool-local cap

[tools.background_jobs]
stalled_warning_seconds = 900   # warn once per background job after this many quiet seconds; 0 disables

[lsp]
enabled = true   # language server tools; servers launch lazily when used
# [lsp.servers.go]
# command = "gopls"
# args = []
# extensions = [".go"]

[skills]
# paths = ["~/my-skills", "../shared/skills"]   # extra custom skill roots
# excluded_paths = ["~/.agents/skills"]   # hide convention roots without deleting folders
# max_depth = 3   # nested scan depth; set 1 for legacy root-only discovery
# disabled_skills = ["review"]   # hide noisy or unwanted skills

[permissions]
# Per-call gating. mode = writer fallback when no rule matches: ask|allow|deny.
# Readers always default to allow. Precedence: deny > ask > allow > fallback.
# Rules are "Tool" or "Tool(specifier)"; e.g. Bash(go test:*), Edit(src/**).
mode  = "ask"
# deny = ["Bash(rm -rf*)", "Bash(git push*)"]   # hard-blocked in every mode
allow = ["Bash(cd /Users/kuangshensheng/codes/UnderTheHeaven && git remote -v)", "Bash(cd /Users/kuangshensheng/codes/UnderTheHeaven && git status)", "Bash(cd /Users/kuangshensheng/codes/UnderTheHeaven && git log --oneline -5)", "Bash=cd /Users/kuangshensheng/Downloads/Four_Symbols_split && python3 -c \"\nfrom struct import unpack\nimport os\nfor f in sorted(os.listdir('.')):\n    if not f.endswith('.png'): continue\n    with open(f,'rb') as fh:\n        d = fh.read(40)\n    w,h = unpack('>II', d[16:24])\n    # check alpha channel in IHDR color type\n    color_type = d[25]\n    print(f, w, 'x', h, 'color_type=', color_type)\n\"", "Bash=cd /Users/kuangshensheng/codes/UnderTheHeaven && cat > /tmp/check_seal.mjs <<'EOF'\nimport { createRng } from './src/engine/MapGenerator.ts';\nlet cards = 0, total = 0;\nfor (let seed = 1; seed <= 200; seed++) {\n  const rng = createRng(seed);\n  const wantHeal = rng() < 0.3;\n  const charCount = Math.min(1 + Math.floor(rng() * 2), 9, wantHeal ? 2 : 3);\n  for (let i = 0; i < charCount; i++) rng();\n  const slotCount = 4 - charCount - (wantHeal ? 1 : 0);\n  for (let i = 0; i < slotCount; i++) {\n    total++;\n    if (rng() < 0.35) cards++;\n  }\n}\nconsole.log('cards:', cards, '/', total, 'slots', (cards/total).toFixed(3));\nEOF\nnpx vitest run --reporter=basic 2>/dev/null | head -1; node --experimental-strip-types /tmp/check_seal.mjs 2>&1 | tail -3 || node -e \"console.log('node too old')\"", "Bash=cd /Users/kuangshensheng/codes/UnderTheHeaven && node -e \"\nfunction createRng(seed) {\n  let a = seed >>> 0;\n  return () => {\n    a |= 0;\n    a = (a + 0x6d2b79f5) | 0;\n    let t = Math.imul(a ^ (a >>> 15), 1 | a);\n    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;\n    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;\n  };\n}\nlet cards = 0, total = 0, seedsWithCard = 0;\nfor (let seed = 1; seed <= 200; seed++) {\n  const rng = createRng(seed);\n  const wantHeal = rng() < 0.3;\n  const charCount = Math.min(1 + Math.floor(rng() * 2), 9, wantHeal ? 2 : 3);\n  for (let i = 0; i < charCount; i++) rng();\n  const slotCount = 4 - charCount - (wantHeal ? 1 : 0);\n  let has = false;\n  for (let i = 0; i < slotCount; i++) {\n    total++;\n    if (rng() < 0.35) { cards++; has = true; }\n  }\n  if (has) seedsWithCard++;\n}\nconsole.log('cards slots:', cards, '/', total, '=', (cards/total).toFixed(3));\nconsole.log('seeds with card:', seedsWithCard, '/200');\n\"", "Bash=cd /Users/kuangshensheng/codes/UnderTheHeaven && for f in public/sfx_play_card.mp3 public/sfx_bomb.mp3 public/bgm_menu_44100.mp3; do echo \"== $f\"; ffprobe -v error -show_entries stream=codec_name,sample_rate,channels -of default=noprint_wrappers=1 \"$f\" 2>&1; done; echo \"== source\"; ffprobe -v error -show_entries stream=codec_name,sample_rate,channels,duration -of default=noprint_wrappers=1 \"/Users/kuangshensheng/Downloads/冰裂碎裂破碎情绪破裂_爱给网_aigei_com.mp3\" 2>&1", "Bash=which pdftotext; python3 -c \"import fitz\" 2>&1 | head -1; python3 -c \"import pdfplumber\" 2>&1 | head -1; python3 -c \"import PyPDF2\" 2>&1 | head -1", "Bash=if [ -n \"$VOLCENGINE_API_KEY\" ]; then echo \"KEY 已设置 (${#VOLCENGINE_API_KEY} chars)\"; else source ~/.bash_profile 2>/dev/null; if [ -n \"$VOLCENGINE_API_KEY\" ]; then echo \"source 后 KEY 已设置 (${#VOLCENGINE_API_KEY} chars)\"; else echo \"KEY 未设置\"; fi; fi", "Bash=cd /Users/kuangshensheng/codes/UnderTheHeaven && python3 -c \"\nfrom PIL import Image\nimport glob\nfor f in sorted(glob.glob('public/char_*.png'))[:4]:\n    im = Image.open(f)\n    print(f, im.size)\n\" 2>/dev/null || echo \"PIL not available\"", "Bash=ls .reasonix/skills/seedream-image/scripts/ 2>/dev/null; echo \"---\"; env | grep -i volcengine | sed 's/=.*/=<set>/' ; echo \"---\"; ls public/char_*.png | head -30; echo \"---\"; ls docs/design/game/", "Bash=source ~/.bash_profile && python3 .reasonix/skills/seedream-image/scripts/seedream_generate.py \\\n  --prompt \"《三国志》游戏系列厚涂风格的人物头部肖像，画面比例 1:1，明朝内阁首辅张居正，中年文官，面容清瘦冷峻，目光锐利深沉，头戴乌纱帽，身着深色朝服，威严果决的气质，厚涂油画质感，硬朗写实光影，面部细节清晰，背景为深色古典朝堂氛围，竖构图居中，人物头部与肩部占画面主体，不要任何文字，不要现代元素。\" \\\n  --size \"1024x1024\" \\\n  --image-format png \\\n  --watermark \\\n  --output /tmp/char_zhangjuzheng_raw.png", "Bash=source ~/.bash_profile 2>/dev/null; if [ -n \"$VOLCENGINE_API_KEY\" ]; then echo \"VOLCENGINE_API_KEY: set (len=${#VOLCENGINE_API_KEY})\"; else echo \"VOLCENGINE_API_KEY: NOT set\"; fi; ls .reasonix/skills/seedream-image/scripts/ 2>/dev/null; ls public/char_zhangjuzheng.png 2>/dev/null && echo \"sample avatar exists\"", "Bash=cd /Users/kuangshensheng/codes/UnderTheHeaven && source ~/.bash_profile 2>/dev/null; python3 .reasonix/skills/seedream-image/scripts/seedream_generate.py --prompt \"《三国志》游戏系列厚涂风格的人物头部肖像，画面比例 1:1，少年侠客，二十出头，束发，剑眉星目，目光如炬，面容刚毅，身穿褐色布衣短打，肩挎长剑，神情豪迈自信，厚涂油画质感，硬朗写实光影，面部细节清晰，背景为深色古典山水夜色氛围，竖构图居中，人物头部与肩部占画面主体，不要任何文字，不要现代元素。\" --size \"1024x1024\" --image-format png --watermark --output /tmp/char_zhouchu_raw.png", "Bash=source ~/.bash_profile 2>/dev/null; if [ -n \"$VOLCENGINE_API_KEY\" ]; then echo \"VOLCENGINE_API_KEY 已设置\"; else echo \"VOLCENGINE_API_KEY 未设置\"; fi; ls public/char_*.png | head -50; echo \"---\"; ls src/skills/", "Bash=source ~/.bash_profile 2>/dev/null; if [ -n \"$VOLCENGINE_API_KEY\" ]; then echo \"VOLCENGINE_API_KEY is set (len ${#VOLCENGINE_API_KEY})\"; else echo \"VOLCENGINE_API_KEY NOT set\"; fi; ls /tmp/char_*.png 2>/dev/null | head; python3 -c \"import PIL; print('PIL ok')\" 2>&1", "Bash=source ~/.bash_profile 2>/dev/null; python3 .reasonix/skills/seedream-image/scripts/seedream_generate.py \\\n  --prompt \"《三国志》游戏系列厚涂风格的人物头部肖像，画面比例 1:1，\n秦国相邦吕不韦，中年男子，面如冠玉，眉目深沉锐利，蓄短须，头戴黑色相邦冠冕，身着黑金相间的华贵朝服，气质雍容威严，富商般的精明神态，\n厚涂油画质感，硬朗写实光影，面部细节清晰，\n背景为深色古典殿堂氛围，竖构图居中，人物头部与肩部占画面主体，不要任何文字，不要现代元素。\" \\\n  --size \"1024x1024\" \\\n  --image-format png \\\n  --watermark \\\n  --output /tmp/char_lvbuwei_raw.png", "Bash=cd /Users/kuangshensheng/codes/UnderTheHeaven && python3 - <<'EOF'\nfrom PIL import Image\nim = Image.open('/tmp/char_lvbuwei_raw.png').convert('RGB')\nim = im.resize((512, 512), Image.LANCZOS)\nim.save('public/char_lvbuwei.png', optimize=True)\nprint(im.size, im.mode)\nEOF", "Bash=cd /Users/kuangshensheng/codes/UnderTheHeaven && for c in 40863b5 a9e4c3e c42a787; do echo \"=== $c ===\"; git show $c:src/skills/LiuBoWenChouSuan.ts 2>/dev/null | grep -n \"dialog\\|台词\\|showDialog\\|execute\" | head -8; done"]
# ask = ["Edit(src/**)"]   # force a prompt even if otherwise allowed

[sandbox]
# Confine tool blast radius. File-writers (write_file/edit_file/multi_edit/move_file)
# may only write under workspace_root (empty = current dir) and allow_write extras.
# bash = "enforce" (default) jails each command in an OS sandbox (macOS now;
# graceful fallback elsewhere); "off" disables it. network allows egress.
# workspace_root = ""            # default: current working directory
# allow_write = ["/tmp"]          # extra dirs writers may also modify
bash    = "enforce"
network = true

[statusline]
# A custom status line: a command whose first stdout line replaces the built-in
# data row. It receives {"model","contextUsed","contextWindow","cwd"} as JSON on stdin.
# command = "my-statusline.sh"

# External MCP servers. type: "stdio" (default, a subprocess) | "http" | "sse".
# ${VAR} / ${VAR:-default} are expanded from the environment in command/args/env/url/headers.
# [[plugins]]
# name    = "example"
# command = "reasonix-plugin-example"
# [[plugins]]                                  # a remote server over Streamable HTTP
# name    = "stripe"
# type    = "http"
# url     = "https://mcp.stripe.com"
# headers = { Authorization = "Bearer ${STRIPE_KEY}" }
